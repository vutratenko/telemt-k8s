apiVersion: v1
kind: Service
metadata:
  name: telemt
  labels:
    app.kubernetes.io/name: telemt
    app.kubernetes.io/part-of: telemt-k8s
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: telemt
  ports:
    - name: mtproxy
      port: 8443
      targetPort: mtproxy
      protocol: TCP
